# PRD.md

# Responsive Weather PRD

## 1. 제품 개요

**Responsive Weather**는 사용자가 세계지도를 탐색하며 특정 국가 또는 지역을 선택하면, 해당 지역의 상세 날씨를 확인할 수 있는 반응형 웹 애플리케이션이다.

이 서비스의 핵심은 단순히 날씨 정보를 보여주는 것이 아니라, **날씨 상태에 따라 UI 자체가 반응하는 경험**을 제공하는 것이다.

예를 들어 비가 오는 지역을 선택하면 우측 상세 패널과 버튼이 비 오는 분위기로 바뀌고, 맑은 지역을 선택하면 햇빛이 느껴지는 밝은 UI로 변경된다.

---

## 2. 제품 목표

Responsive Weather의 목표는 다음과 같다.

1. 사용자가 세계지도를 통해 직관적으로 지역을 선택할 수 있게 한다.
2. 선택한 국가 또는 지역의 날씨 정보를 상세하게 보여준다.
3. 날씨 상태에 따라 화면 분위기, 버튼, 카드, 패널이 반응하도록 만든다.
4. 데스크톱, 태블릿, 모바일 환경에서 모두 자연스럽게 사용할 수 있는 반응형 UI를 제공한다.
5. 추후 실제 날씨 API, 국가 검색, 즐겨찾기, 위치 기반 날씨 등으로 확장 가능한 구조를 갖춘다.

---

## 3. 타깃 사용자

이 제품의 주요 사용자는 다음과 같다.

- 세계 여러 지역의 날씨를 시각적으로 보고 싶은 사용자
- 일반적인 날씨 앱보다 더 인터랙티브한 웹 경험을 원하는 사용자
- 지도 기반으로 날씨를 탐색하고 싶은 사용자
- 날씨 상태에 따라 변화하는 감성적인 UI를 경험하고 싶은 사용자

---

## 4. 핵심 사용자 경험

사용자는 처음 웹사이트에 접속하면 대시보드 형태의 Home 화면을 본다.

Home 화면에는 2D 지구본 또는 세계지도 형태의 인터랙티브 지도가 표시된다.

사용자는 지도에서 특정 나라 또는 지역을 클릭할 수 있다.

지역을 클릭하기 전에는 지도가 화면의 중심을 차지한다.

지역을 클릭하면 지도 영역은 약 70% 너비로 줄어들고, 우측에 선택한 지역의 상세 날씨 패널이 나타난다.

날씨 상세 패널에는 선택한 지역의 날씨 정보가 표시되며, 날씨 상태에 따라 UI 스타일이 변경된다.

---

## 5. 주요 화면

## 5.1 Home Dashboard

Home은 이 서비스의 메인 화면이다.

Home 화면은 다음 요소를 포함한다.

- 2D 세계지도 또는 지구본 스타일 지도
- 지도 확대 버튼
- 지도 축소 버튼
- 국가 또는 지역 선택 기능
- 선택된 지역이 있을 경우 우측 날씨 상세 패널
- 날씨 상태에 따라 반응하는 UI

---

## 5.2 지도 영역

지도 영역은 사용자가 세계의 국가 또는 지역을 선택하는 핵심 인터페이스다.

지도는 다음 기능을 제공해야 한다.

- 세계지도 형태로 표시된다.
- `+` 버튼을 누르면 확대된다.
- `-` 버튼을 누르면 축소된다.
- 특정 국가 또는 지역을 클릭할 수 있다.
- 지도 위에는 날씨 정보를 직접 표시하지 않는다.
- 날씨 정보는 사용자가 국가 또는 지역을 클릭한 뒤 우측 패널에 표시한다.

---

## 5.3 날씨 상세 패널

사용자가 국가 또는 지역을 선택하면 우측에 날씨 상세 패널이 표시된다.

상세 패널은 다음 정보를 포함할 수 있다.

- 선택한 국가 또는 지역명
- 현재 온도
- 날씨 상태
- 체감 온도
- 습도
- 풍속
- 간단한 날씨 설명
- 날씨 상태에 맞는 시각적 효과

초기 MVP에서는 실제 API 대신 mock 데이터를 사용할 수 있다.

---

## 6. 레이아웃 요구사항

## 6.1 지역 선택 전

지역이 선택되지 않은 상태에서는 지도가 화면의 중심을 차지한다.

```txt
[                 World Map 100%                 ]
```

이 상태에서는 우측 상세 패널을 표시하지 않는다.

---

## 6.2 지역 선택 후

지역이 선택되면 지도는 약 70% 영역으로 줄어들고, 우측에는 날씨 상세 패널이 표시된다.

```txt
[          World Map 70%          ][ Weather Panel 30% ]
```

---

## 6.3 모바일 화면

모바일에서는 가로 공간이 부족하므로 지도와 상세 패널을 세로로 배치한다.

```txt
[ World Map ]

[ Weather Detail Panel ]
```

또는 추후에는 상세 패널을 Bottom Sheet 또는 Drawer 형태로 표시할 수 있다.

---

## 7. 날씨별 UI 반응

Responsive Weather의 핵심 차별점은 날씨에 따라 UI가 반응하는 것이다.

날씨 상태는 최소한 다음 값을 고려한다.

```ts
type WeatherCondition =
  | "sunny"
  | "rainy"
  | "cloudy"
  | "snowy"
  | "stormy"
  | "foggy"
  | "unknown";
```

---

## 7.1 맑음

맑은 날씨일 경우 다음과 같은 UI를 적용한다.

- 밝고 따뜻한 배경
- 햇빛 느낌의 gradient
- 버튼에 glow 효과
- 노란색 또는 따뜻한 색감
- 카드와 패널이 밝고 부드러운 분위기를 갖도록 표현

---

## 7.2 비

비가 오는 날씨일 경우 다음과 같은 UI를 적용한다.

- 어두운 파란색 또는 회색 계열 배경
- 비가 내리는 듯한 미세한 애니메이션
- 버튼에 빗방울 또는 물결 느낌의 효과
- 상세 패널 전체가 비 오는 분위기를 갖도록 표현

---

## 7.3 흐림

흐린 날씨일 경우 다음과 같은 UI를 적용한다.

- 부드러운 회색 또는 푸른 계열 배경
- 구름 느낌의 흐릿한 gradient
- 강한 대비보다 차분한 분위기
- 카드에 soft shadow 적용

---

## 7.4 눈

눈이 오는 날씨일 경우 다음과 같은 UI를 적용한다.

- 밝고 차가운 색감
- 흰색, 하늘색 계열 사용
- 눈 입자 느낌의 효과
- 부드러운 border와 blur 효과

---

## 7.5 천둥/폭풍

천둥 또는 폭풍 날씨일 경우 다음과 같은 UI를 적용한다.

- 어두운 배경
- 강한 대비
- 번개 느낌의 포인트 효과
- 단, 사용성을 해치지 않도록 과도한 애니메이션은 피한다.

---

## 8. MVP 범위

초기 MVP에서는 다음 기능을 우선 구현한다.

- Home 대시보드 화면
- 2D 세계지도 UI
- 지도 확대/축소 기능
- 국가 또는 지역 클릭 기능
- 선택된 국가/지역 상태 관리
- 선택 후 지도 70% + 우측 패널 30% 레이아웃
- 우측 날씨 상세 패널
- mock 날씨 데이터
- sunny, rainy, cloudy 기본 날씨 UI variant
- shadcn/ui 기반 버튼, 카드, 패널
- Tailwind CSS 기반 반응형 레이아웃

---

## 9. MVP에서 제외할 기능

초기 MVP에서는 다음 기능을 제외할 수 있다.

- 실제 위치 기반 날씨
- 사용자 로그인
- 즐겨찾기
- 날씨 검색 자동완성
- 모든 국가의 완전한 날씨 데이터
- 복잡한 지도 레이어
- 고급 3D 지구본
- 다국어 지원
- PWA
- 알림 기능
- 장기 예보
- 시간대별 예보

---

## 10. 기술 방향

이 프로젝트는 다음 기술을 사용한다.

- Next.js 최신 버전
- TypeScript
- Tailwind CSS
- shadcn/ui
- npm

프론트엔드는 Next.js App Router 기반으로 구성한다.

UI는 shadcn/ui 컴포넌트를 우선 사용하고, Tailwind CSS로 커스터마이징한다.

인터랙션이 필요한 지도, 확대/축소 버튼, 국가 선택, 날씨 패널 전환은 Client Component로 구현한다.

---

## 11. 데이터 요구사항

초기 MVP에서는 mock 데이터를 사용할 수 있다.

mock 데이터는 다음 구조를 가질 수 있다.

```ts
type WeatherData = {
  countryCode: string;
  countryName: string;
  temperature: number;
  feelsLike?: number;
  humidity?: number;
  windSpeed?: number;
  condition: WeatherCondition;
  description: string;
};
```

추후 실제 날씨 API를 연결할 경우 API 응답을 그대로 UI에 사용하지 않고, 앱 내부에서 사용하는 날씨 데이터 타입으로 변환해야 한다.

---

## 12. 주요 사용자 플로우

## 12.1 첫 접속

1. 사용자가 웹사이트에 접속한다.
2. Home 대시보드가 표시된다.
3. 세계지도가 화면 중앙에 표시된다.
4. 사용자는 지도 확대/축소 버튼을 사용할 수 있다.

---

## 12.2 국가 선택

1. 사용자가 지도에서 특정 국가를 클릭한다.
2. 선택한 국가 정보가 상태에 저장된다.
3. 지도는 화면의 약 70% 영역으로 줄어든다.
4. 우측에 날씨 상세 패널이 표시된다.
5. 선택한 국가의 날씨 정보가 표시된다.
6. 날씨 상태에 따라 패널, 버튼, 카드 스타일이 변경된다.

---

## 12.3 다른 국가 선택

1. 사용자가 지도에서 다른 국가를 클릭한다.
2. 선택된 국가가 변경된다.
3. 우측 상세 패널의 날씨 정보가 변경된다.
4. 날씨 상태가 다르면 UI 분위기도 함께 변경된다.

---

## 13. 성공 기준

MVP가 성공적으로 구현되었다고 판단하는 기준은 다음과 같다.

- 사용자가 Home 화면에서 세계지도를 볼 수 있다.
- 사용자가 `+`, `-` 버튼으로 지도를 확대/축소할 수 있다.
- 사용자가 국가 또는 지역을 클릭할 수 있다.
- 국가 또는 지역 클릭 시 우측 상세 패널이 표시된다.
- 지역 선택 후 지도와 패널이 데스크톱에서 70:30 구조로 표시된다.
- 모바일에서는 지도와 패널이 자연스럽게 세로 배치된다.
- 날씨 상태에 따라 버튼, 카드, 패널의 UI가 다르게 표시된다.
- mock 데이터를 기반으로 최소 3가지 날씨 상태가 표현된다.
- TypeScript 기반으로 안정적인 타입 구조를 가진다.
- 추후 실제 날씨 API를 연결하기 쉬운 구조를 가진다.

---

## 14. 향후 확장 기능

MVP 이후 다음 기능을 추가할 수 있다.

- 실제 날씨 API 연동
- 현재 위치 기반 날씨 보기
- 국가 또는 도시 검색
- 즐겨찾기 지역 저장
- 최근 본 지역
- 시간대별 날씨
- 주간 날씨
- 지도 위 간단한 날씨 마커 표시 옵션
- 다크모드
- 다국어 지원
- PWA 지원
- 날씨별 고급 애니메이션
- 사용자 설정 저장

---

## 15. 제품 방향성

Responsive Weather는 단순한 날씨 정보 앱이 아니다.

이 제품은 사용자가 세계지도를 통해 지역을 탐색하고, 선택한 지역의 날씨가 UI 분위기 전체에 반영되는 경험을 제공하는 인터랙티브 날씨 웹 애플리케이션이다.

따라서 모든 기능은 다음 기준을 중심으로 설계한다.

1. 지도 탐색이 직관적인가?
2. 지역 선택 후 날씨 정보가 명확하게 보이는가?
3. 날씨 상태가 UI에 자연스럽게 반영되는가?
4. 반응형 환경에서도 사용하기 편한가?
5. 추후 기능 확장이 쉬운 구조인가?
